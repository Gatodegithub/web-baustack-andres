import * as React from "react"

import { LogoContainer } from './NavbarLogo.elements';
import { Logo } from '../../../SVG';

const NavbarLogo = () => {
  return (
    <LogoContainer>
      <Logo />
    </LogoContainer>
  )
}

export default NavbarLogo
